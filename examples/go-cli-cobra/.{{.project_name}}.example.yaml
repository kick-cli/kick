{{- if .include_config}}
# Example configuration file for {{.project_name}}
# Copy this to ~/.{{.project_name}}.yaml or ./.{{.project_name}}.yaml

# Application settings
verbose: false
debug: false

# Output format
output_format: "text"  # Options: text, json, yaml

# Default values for commands
defaults:
  greeting: "Hello"
  language: "en"

# Feature flags
features:
  colors: true
  timestamps: true
{{- end}}